/* global OO */
( function( $, OO ) {
	"use strict";
	$( function() {
		// infusing the DropdownInputWidgets makes
		// them look prettier.
		OO.ui.infuse( 'mw-extdist-selector' );
	} );
}( jQuery, OO ) );
